pack_version: 1

scope:
  repo: rmembr
  namespace: default

owners:
  - rmembr-maintainers

required_files:
  - instructions.md

classification: internal

embedding:
  model: nomic-embed-text
  dims: 768
  version: locked

references:
  standards: []

override_policy:
  allow_repo_overrides: false

